import { ReactElement } from "react";

export type TopbarProps = {
  text?: string;
  styleFont?: string;
  colorFont?: string;
  logo?: ReactElement;
  button?: ReactElement;
  button2?: ReactElement;
  icone?: ReactElement;
  arrowLeft?: ReactElement;
};
